# Managed process lifecycle

Exercises process input, output, waiting, PTY resizing, and termination. See [main.go](main.go) for the code.

From the repository root:

```sh
export CREATEOS_API_KEY="your-api-key"
go run ./examples/managed-process
```

This example creates live resources and cleans them up when it finishes.
