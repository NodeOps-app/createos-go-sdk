# Desktop and noVNC

Exercises screenshot, mouse, clipboard, browser, and noVNC operations. See [main.go](main.go) for the code.

From the repository root:

```sh
export CREATEOS_API_KEY="your-api-key"
go run ./examples/desktop
```

This example creates live resources and cleans them up when it finishes.
