# Files and snapshots

Uploads a file, forks a sandbox, and checks inherited and independent file state. See [main.go](main.go) for the code.

From the repository root:

```sh
export CREATEOS_API_KEY="your-api-key"
go run ./examples/files-and-snapshots
```

This example creates live resources and cleans them up when it finishes.
