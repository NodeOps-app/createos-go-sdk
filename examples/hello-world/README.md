# Hello world

Creates a sandbox, runs a command, and destroys the sandbox. See [main.go](main.go) for the code.

From the repository root:

```sh
export CREATEOS_API_KEY="your-api-key"
go run ./examples/hello-world
```

This example creates live resources and cleans them up when it finishes.
